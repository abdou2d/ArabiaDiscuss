<%= form_for(post) do |f| %>
  <%= f.text_field :title, placeholder: "عنوان يصف الموضوع بشكل كافي" %>
  <%= f.text_area :content, placeholder: "محتوى الموضوع" %>
  <%= f.submit "نَشْر", class: "btn post_submit" %>
<% end %>
